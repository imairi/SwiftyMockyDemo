source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '11.0'
install! 'cocoapods', :generate_multiple_pod_projects => true, :incremental_installation => true

use_frameworks!

target 'SwiftyMockyDemoTests' do
  inherit! :complete

  pod 'SwiftyMocky'
  pod 'Fakery'
end
